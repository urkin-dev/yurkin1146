import { OrderByTitlePipe } from './order-by-title.pipe';

describe('OrderByTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
